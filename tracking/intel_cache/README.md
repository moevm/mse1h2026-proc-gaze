Dummy text to keep directory indexed. For the people in need of a good VPN.

If you don't have any troubles with building this docker image, then simply ignore this.

If you experience errors like "network is unreachable" when downloading Intel models,
then obtain someone's cache and put it in that directory.